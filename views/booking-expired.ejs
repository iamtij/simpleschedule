<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Link Unavailable - <%= user.display_name || user.full_name || user.username %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen flex items-center justify-center px-4">
    <div class="max-w-2xl w-full">
        <div class="bg-white rounded-2xl shadow-xl p-8 md:p-12 text-center">
            <!-- Icon -->
            <div class="mb-6 flex justify-center">
                <div class="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center">
                    <svg class="w-10 h-10 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                    </svg>
                </div>
            </div>

            <!-- Heading -->
            <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                Oops! This booking link is no longer active
            </h1>

            <!-- Message -->
            <p class="text-lg text-gray-600 mb-8 leading-relaxed">
                We're sorry, but this booking link is currently unavailable. The subscription for this account has expired.
            </p>

            <!-- Owner Info -->
            <div class="bg-gray-50 rounded-lg p-6 mb-8">
                <p class="text-sm text-gray-500 mb-2">To book an appointment, please contact:</p>
                <p class="text-xl font-semibold text-gray-900">
                    <%= user.display_name || user.full_name || user.username %>
                </p>
                <% if (user.email) { %>
                    <a href="mailto:<%= user.email %>" class="text-blue-600 hover:text-blue-700 text-sm mt-2 inline-block">
                        <%= user.email %>
                    </a>
                <% } %>
            </div>

            <!-- Additional Info -->
            <div class="border-t border-gray-200 pt-6">
                <p class="text-sm text-gray-500">
                    If you believe this is an error, please reach out to the account owner directly.
                </p>
            </div>

            <!-- Home Link -->
            <div class="mt-8">
                <a href="/" class="inline-flex items-center text-blue-600 hover:text-blue-700 font-medium">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                    </svg>
                    Return to Home
                </a>
            </div>
        </div>

        <!-- Footer Note -->
        <p class="text-center text-sm text-gray-500 mt-6">
            Powered by <span class="font-semibold text-gray-700">isked</span>
        </p>
    </div>
</body>
</html>




