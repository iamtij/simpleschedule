<style>
  /* Landing uses brand-600 #7c3aed — Tailwind v2 CDN omits `violet-*`, so primary actions need explicit colors */
  .auth-btn-primary {
    background-color: #7c3aed;
    color: #ffffff;
    border: none;
  }
  .auth-btn-primary:hover {
    background-color: #6d28d9;
  }
  .auth-btn-primary:focus {
    outline: none;
  }
  .auth-btn-primary:focus-visible {
    box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.45);
  }
  .auth-link {
    color: #6d28d9;
  }
  .auth-link:hover {
    color: #5b21b6;
  }
</style>
